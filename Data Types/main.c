#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;
    double gpa = 7.9;
    char grade = 'A';
    char phrase[] = "Giraffe Academy";

    printf(age);
    printf(gpa);
    printf(grade);
    printf(phrase);
    return 0;
}
